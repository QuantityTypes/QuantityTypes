<Window x:Class="WpfExample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WPF Example" Height="350" Width="525">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Width" Value="160"/>
        </Style>
    </Window.Resources>
    <Grid>
        <UniformGrid Columns="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label Content="Length:"/>
            <TextBox Text="{Binding Length}"/>
            <Label Content="Time:"/>
            <TextBox Text="{Binding Time}"/>
            <Label Content="Velocity:"/>
            <TextBox Text="{Binding Velocity, Mode=OneWay, StringFormat='0.00'}"/>
            <Label Content=""/>
            <TextBox Text="{Binding Velocity, Mode=OneWay,StringFormat='0.00 km/h'}"/>
        </UniformGrid>
    </Grid>
</Window>
